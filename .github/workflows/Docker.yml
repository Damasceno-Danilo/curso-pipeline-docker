name: Docker

on:
 workflow_call:

jobs:

  docker:
    runs-on: ubuntu-latest
    
   
    steps:
    - uses: actions/checkout@v3

    - name: Setup Docker build
      uses: docker/setup-buildx-action@v4.2.0

     - name: Docker Login
       uses: docker/login-action@v4.6.0
       with:
   
    username: danilodamasceno

    password: ${{secrets.PASSWORD_DOCKER_HUB}}
